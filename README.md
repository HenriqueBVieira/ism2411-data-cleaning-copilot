This is a code that getts messy data from a CSV file and cleans it up and puts the data into a cleaned csv data file. 

How to use:

Install pandas library

run the code: python src/data_cleaning.py

The clean data should go to: data/processed/sales_data_clean.csv